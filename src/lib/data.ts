import { Project, Article } from "@/types";

// Legacy local data (kept only if needed elsewhere). Prefer using i18n data via usePortfolioData.

export const projects: Project[] = [];

export const articles: Article[] = [];
